import React,{useCallback,useEffect,useState} from 'react';import {ActivityIndicator,Pressable,ScrollView,StyleSheet,Text,View} from 'react-native';import {get,post} from '../api/client';import {NotificationCard} from '../app/types';import {C} from '../theme';import ProtectedImage from '../components/ProtectedImage';
export default function NotificationsScreen({onBack,onTarget,onCount}:{onBack:()=>void;onTarget:(t:NonNullable<NotificationCard['target']>)=>void;onCount:(n:number)=>void}){const [items,setItems]=useState<NotificationCard[]>([]);const [unread,setUnread]=useState(0);const [loading,setLoading]=useState(true);const [error,setError]=useState('');const load=useCallback(async()=>{try{setLoading(true);const r=await get('/notifications');setItems(r.data||[]);setUnread(Number(r.unread_count||0));onCount(Number(r.unread_count||0));}catch(e:any){setError(String(e?.message||e));}finally{setLoading(false)}},[]);useEffect(()=>{load()},[load]);const markAll=async()=>{await post('/notifications/read-all',{});setUnread(0);setItems(v=>v.map(n=>({...n,read:true})));onCount(0)};return <ScrollView style={s.page} contentContainerStyle={s.content}><View style={s.top}><Pressable onPress={onBack}><Text style={s.back}>‹ Back</Text></Pressable><Text style={s.title}>Notifications</Text><Pressable disabled={!unread} onPress={markAll}><Text style={[s.read,!unread&&{opacity:.35}]}>Mark read</Text></Pressable></View><Text style={s.lead}>{unread?`${unread} unread update${unread===1?'':'s'}`:'You’re all caught up.'}</Text>{error?<Text style={s.error}>{error}</Text>:null}{loading?<ActivityIndicator color={C.gold} style={{marginTop:50}}/>:items.map(n=><Pressable disabled={!n.target} onPress={()=>n.target&&onTarget(n.target)} key={n.id} style={[s.row,!n.read&&s.unread]}>{n.avatar?<ProtectedImage uri={n.avatar} fallback={n.actor_name||n.title} style={s.avatar}/>:<View style={s.icon}><Text style={s.iconText}>{n.icon||'PG'}</Text></View>}<View style={{flex:1}}><View style={s.line}><Text numberOfLines={1} style={s.name}>{n.title}</Text><Text style={s.time}>{n.time}</Text></View><Text style={s.cat}>{n.category_label}</Text>{n.message?<Text numberOfLines={2} style={s.message}>{n.message}</Text>:null}</View>{!n.read&&<View style={s.dot}/>}</Pressable>)}</ScrollView>}
const s=StyleSheet.create({page:{flex:1,backgroundColor:C.bg},content:{padding:16,paddingBottom:30},top:{height:56,flexDirection:'row',alignItems:'center',justifyContent:'space-between'},back:{color:C.gold2,fontWeight:'800'},title:{color:C.text,fontWeight:'900',fontSize:18},read:{color:C.gold2,fontWeight:'800',fontSize:11},lead:{color:C.muted,fontSize:12,marginBottom:14},error:{color:'#ff8d8d'},row:{flexDirection:'row',gap:11,padding:13,borderRadius:17,borderWidth:1,borderColor:C.line,backgroundColor:C.panel,marginBottom:8,alignItems:'center'},unread:{backgroundColor:'#171912',borderColor:'#4c4529'},avatar:{width:46,height:46,borderRadius:15},icon:{width:46,height:46,borderRadius:15,backgroundColor:C.panel3,alignItems:'center',justifyContent:'center'},iconText:{color:C.gold2,fontWeight:'900'},line:{flexDirection:'row',justifyContent:'space-between',gap:8},name:{flex:1,color:C.text,fontWeight:'900',fontSize:13},time:{color:C.faint,fontSize:9},cat:{color:C.gold,fontSize:9,fontWeight:'800',marginTop:2},message:{color:C.muted,fontSize:11,lineHeight:15,marginTop:4},dot:{width:8,height:8,borderRadius:4,backgroundColor:C.gold}});
